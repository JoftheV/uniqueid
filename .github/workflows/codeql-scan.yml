name: Dispatch Smoke Test

on:
  workflow_dispatch:
  push:
    branches: [ "master" ]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "dispatch ok"
          date
